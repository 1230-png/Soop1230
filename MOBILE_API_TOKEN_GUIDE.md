# 📱 모바일 환경용 구글 API OAuth 토큰 추출 스크립트

클로드(Claude) 코드나 Termux, 리플릿(Replit) 등 모바일 기반 개발 환경에서 실행하여 구글 브랜드 계정의 `refresh_token`을 추출하는 독립형 파이썬 스크립트입니다.

## 1. 사전 준비 (모바일 크롬 브라우저 필수)

구글 클라우드 콘솔(GCP)에서 OAuth 2.0 클라이언트 ID를 생성하고 `client_secret.json` 파일을 다운로드 받아 작업 디렉토리에 저장하세요.

* **애플리케이션 유형:** 데스크톱 앱 (Desktop App)
* **주의:** 로그인 승인 화면이 뜰 때 반드시 본인 계정 하위의 **[브랜드 계정]**을 터치하여 선택해야 합니다.

## 2. 필수 라이브러리 설치

모바일 터미널(Termux 등) 환경에서 아래 명령어를 실행하여 구글 인증 라이브러리를 설치합니다.

```bash
pip install google-auth-oauthlib google-auth-httplib2 google-api-python-client
```

## 3. 스크립트 실행

제공된 `get_tokens.py` 파일을 실행하세요.

```bash
python get_tokens.py
```

구글 인증 서버가 주는 로컬 링크를 모바일 크롬에 복사한 뒤, 최종 인증 코드(또는 리디렉션 주소의 `code=` 뒷부분)를 터미널에 입력하면 브랜드 계정 전용 토큰이 발급됩니다.

---

## 📋 상세 실행 가이드

### 1단계: 구글 클라우드 콘솔(GCP) 기본 설정

1. 스마트폰에서 크롬 브라우저를 열고 구글 클라우드 콘솔에 접속합니다.
   - 우측 상단 메뉴에서 '데스크톱 사이트'를 체크하면 조작이 훨씬 수월합니다.

2. 새 프로젝트를 만들고, YouTube Data API v3를 찾아 사용 고도화(Enable)를 누릅니다.

3. 사용자 자격 증명(Credentials) 메뉴 ➡️ 자격 증명 만들기 ➡️ OAuth 클라이언트 ID를 선택합니다.

4. 애플리케이션 유형을 반드시 **데스크톱 앱(Desktop App)**으로 선택한 뒤 생성합니다.

5. 만들어진 키의 JSON 파일을 다운로드하여 이름을 `client_secret.json`으로 바꾼 뒤, 클로드 코드 작업 공간이나 모바일 터미널 디렉토리에 넣어둡니다.

### 2단계: 모바일 환경에서 제공된 스크립트 실행

1. `get_tokens.py` 파일을 실행합니다.
   
   ```bash
   python get_tokens.py
   ```

2. 필요한 라이브러리가 없으면 설치 메시지가 뜹니다. 위의 "필수 라이브러리 설치" 섹션을 참고하세요.

3. 스크립트에서 `open_browser=False` 옵션이 적용되어 있어 자동 브라우저 오픈이 되지 않습니다.

4. 코드를 실행하면 터미널 창에 긴 구글 인증 URL 링크가 텍스트로 출력됩니다.

### 3단계: 브랜드 계정 명확히 선택하기 (🔥 가장 중요)

1. 터미널에 뜬 구글 링크를 꾹 눌러 **전체 복사**한 뒤, 모바일 크롬 브라우저 주소창에 붙여넣고 이동합니다.

2. 구글 로그인 화면이 뜨면 본 계정 이메일을 입력합니다.

3. 이메일 로그인 직후 **"채널(프로필) 선택"** 팝업이 뜹니다.
   - 이때 본 계정이 아닌, **자동화를 돌릴 [브랜드 계정 이름]을 정확하게 터치해야 합니다.**

4. "앱이 안전하지 않음" 경고가 뜨면:
   - 고급 ➡️ ...으로 이동(안전하지 않음)을 누르고 권한 허용을 완료합니다.

5. 허용이 끝나면 웹페이지 주소창에 `code=4/0A...` 형태로 긴 코드가 포함된 주소로 바뀐 채 멈추거나 접속 불가 페이지가 뜹니다.
   - 주소창 전체를 복사하거나 터미널 양식에 맞게 코드를 입력해 주면 깃허브용 `refresh_token`이 모바일 화면에 **즉시 출력**됩니다.

### 4단계: 토큰 저장 및 자동화 구성

1. 출력된 `refresh_token` 값을 복사합니다.

2. GitHub 저장소의 **Settings ➡️ Secrets and variables ➡️ Actions**에서 새로운 Secret을 만듭니다.
   - Secret Name: `YOUTUBE_REFRESH_TOKEN`
   - Secret Value: 위에서 복사한 `refresh_token` 값

3. 이 과정을 채널 개수만큼 반복하면, 추출된 토큰을 통해 깃허브 액션에서 번호 요구 없이 무제한 자동 업로드를 구동할 수 있습니다.

---

## 🔧 구동 환경별 팁

### Termux (Android)
```bash
# 터믹스 설치 및 파이썬 준비
pkg update && pkg upgrade
pkg install python3
pip install google-auth-oauthlib google-auth-httplib2 google-api-python-client
python3 get_tokens.py
```

### Claude Code (웹 기반)
- 직접 파이썬 Artifacts나 인터프리터에서 실행 가능
- `client_secret.json` 파일을 업로드한 후 실행

### Replit (웹 기반)
- Replit의 Shell에서 직접 실행 가능
- 파일 업로드 후 `python get_tokens.py` 실행

---

## ⚠️ 주의사항

- **브랜드 계정 선택 필수**: 일반 계정이 아닌 정확한 브랜드 계정을 선택해야 토큰이 올바르게 생성됩니다.
- **파일명 확인**: `client_secret.json` 파일명은 반드시 정확하게 유지해야 합니다.
- **토큰 보안**: 발급받은 `refresh_token`은 절대 공개 저장소에 커밋하지 마세요. GitHub Secrets를 사용하세요.
- **권한 범위**: 현재 스크립트는 YouTube 업로드 및 채널 관리 권한(`youtube.upload`, `youtube`)을 요청합니다.

---

## 📞 문제 해결

### "client_secret.json이 없습니다" 에러
- GCP에서 다운로드한 JSON 파일의 이름이 정확히 `client_secret.json`인지 확인하세요.
- 파일이 현재 디렉토리에 있는지 확인하세요.

### 브라우저 리디렉션이 실패할 때
- 모바일 크롬에서 "데스크톱 사이트" 모드를 켜고 재시도하세요.
- 주소창의 `code=` 뒷부분을 수동으로 복사해 터미널에 입력하세요.

### "port 8080 is already in use" 에러
- 다른 프로세스가 8080 포트를 사용 중입니다.
- 포트 번호를 변경하고 싶으면 스크립트의 `port=8080` 부분을 다른 번호로 수정하세요.

