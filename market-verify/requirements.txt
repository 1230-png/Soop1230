# 대본 생성
anthropic>=0.40
pandas>=2.0
yfinance>=0.2.50

# 외부 API 직접 호출 — FRED(매크로), 코인게코(희석률), 일레븐랩스(음성).
# 셋 다 함수 안에서 늦게 import 해서 눈에 안 띄지만 없으면 그 자리에서 죽는다.
# 지금은 anthropic·구글 클라이언트가 딸려 오게 두고 있는데, 그쪽이 의존성을
# 바꾸면 매일 도는 무인 발행이 조용히 깨진다. 직접 건다.
requests>=2.31

# 영상 제작
edge-tts>=7.0
# edge-tts 가 막히는 자리(깃허브 러너 등)용 무료 대안
gTTS>=2.5
Pillow>=10.0

# 유튜브 업로드
google-api-python-client>=2.100
google-auth-oauthlib>=1.2

# 테스트
pytest>=8.0

# 이 밖에 시스템에 ffmpeg 와 한글 폰트가 필요하다.
#   Ubuntu : sudo apt-get install -y ffmpeg fonts-noto-cjk
#   Windows: ffmpeg.org 에서 받아 PATH 에 넣는다. 맑은 고딕은 이미 깔려 있다.
